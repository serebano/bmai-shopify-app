# Listing copy — localized ×14

`en.json` is the canonical source. The listing ships translations for all 14 Tier-1
locales (Built-for-Shopify): **en, es, pt-BR, fr, de, it, ru, ro, tr, ar (RTL),
zh-Hans (zh-CN), hi, ja, ko** — same keys as `en.json`, one file per locale
(`es.json`, `pt-BR.json`, …). All 14 are present and carry real translations (not
English placeholders); keep them in sync with `en.json` on every copy change.

**Copy accuracy (Req 4.3.3):** no guarantees, no unverifiable superlatives, no
competitor-flaw positioning. Grounding is described as "grounded, source-cited,
refuses when unsure" — never "no hallucinations". A native review pass before
submission is still recommended.
